# CONTEXT_INJECTION

---

title: CONTEXT_INJECTION

kind: guide

project: Meaning Engine

status: active

scope: now

maturity: canon

---

Короткая смысловая инъекция для быстрой ориентации при смене чата/потере контекста.

## 30 секунд

Meaning Engine/meaning-engine: event‑sourced knowledge + focus‑driven navigation + deterministic projection.

## Формулы

- `G = BuildGraph(Evaluate(Log))`
- `V = P(G, F, C, S, Pr)`
- `UI = R(V)`

## 3 аксиомы

1) Всё выражается в графе (но не всё обязано быть event‑sourced).

2) Focus — операционный центр наблюдения.

3) Projection не изменяет граф.

## Интерактивный цикл

`State=(Focus,Context,Params)`; `State' = T(State,Action)`; `View = P(Graph,State)`.

## Где канон

- AXIOMS, ARCHITECTURE, SPECS.

## Что не путать

- Метафоры/definitions ≠ законы/specs.